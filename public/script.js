const chat=document.getElementById('chat');const form=document.getElementById('chatForm');const prompt=document.getElementById('prompt');const send=document.getElementById('sendBtn');const status=document.getElementById('status');const history=[];function addMessage(role,text){const el=document.createElement('div');el.className=`message ${role}`;el.textContent=text;chat.appendChild(el);chat.scrollTop=chat.scrollHeight}form.addEventListener('submit',async e=>{e.preventDefault();const text=prompt.value.trim();if(!text)return;const prior=[...history];history.push({role:'user',content:text});addMessage('user',text);prompt.value='';status.textContent='';send.disabled=true;send.textContent='…';try{const r=await fetch('/api/chat',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({messages:prior.concat({role:'user',content:text})})});const d=await r.json();if(!r.ok)throw new Error(d.error||'Request failed');addMessage('assistant',d.text||'No response received.');history.push({role:'assistant',content:d.text||''})}catch(err){status.textContent=err.message||'Connection error'}finally{send.disabled=false;send.textContent='Send';prompt.focus()}});document.getElementById('clearBtn').addEventListener('click',()=>{history.length=0;chat.innerHTML='<div class="message assistant">Hello! How can I help you today?</div>';status.textContent=''});prompt.addEventListener('input',()=>{prompt.style.height='auto';prompt.style.height=Math.min(prompt.scrollHeight,180)+'px'});